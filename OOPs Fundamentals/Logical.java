package Operators.java;
public class Logical 
{
    public static void main(String[] args) {
        int a =10;
        int b = 20;
        int c = 30;

        // boolean result =  (a == b && a < b && a > b);
        // boolean result =  (a < b && a <= b && a < c);
        // boolean result =  (10 == 10 || 10 <20 || 10 < 30);
        // boolean result =  (10 < 20 || 10 > 20 || 10 > 50);
        System.out.println(!false);
        // System.out.println(result);
    }    
}
