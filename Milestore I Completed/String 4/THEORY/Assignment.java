package THEORY;
// import java.util.Arrays;

public class Assignment {
    public static void main(String[] args) {
            //😎😎😎😎😎😎😎😎😎😎😎😎
        //appending the Stringbuilder and converting it to the string
        // StringBuffer sb = new StringBuffer ("hello");
        // sb.append(",");
        // sb.append("world!");
        // String result = sb.toString(); 
        // System.out.println(result);


        //🍿🍿🍿🍿🍿🍿🍿🍿🍿
        //Reversing the StringBuilder
        // String input = "PWSKILLS";
        // StringBuilder sb = new StringBuilder(input);
        // sb.reverse();
        // System.out.println(sb.toString());
        



        // //🍚🍚🍚🍚🍚🍚🍚🍚🍚🍚🍚🍚🍚🍚
        // //Reversing a string without using the inbuilt methods of stringbuilder
        // String input = "Think Twice";
        // String input1 = input.toLowerCase(); //think twice
        // String[] words = input1.split(" "); 
        // StringBuilder sb = new StringBuilder(); 
        
        // //Run a loop from the reverse direction
        // for (int i = words.length - 1; i >= 0; i--) {
        //     for (int j = words[i].length() - 1; j >= 0; j--) {

        //         //putting those char in the stringbuilder
        //         sb.append(words[i].charAt(j));
        //     }
        //     //to give a space int between the words
        //     if (i > 0) {
        //         sb.append(" ");
        //     }
        // }
        // //converting Stringbuilder to a string
        // System.out.println(sb.toString());



        

            //🍚🍚🍚🍚🍚🍚🍚🍚🍚🍚
            //Sorting done
            // String input = "Think Twice";
            // String input1 = input.toLowerCase();
            // char[] chArr = input1.toCharArray();
            // Arrays.sort(chArr);
            // String sorted = new String(chArr);
            // System.out.println(sorted);
        
    }
        
}
